## 백준 1181
import sys

N = int(sys.stdin.readline())
lst = []

for i in range(N):
    lst.append(sys.stdin.readline().strip())  #런타임 에러 안나려면 쓰기
    
set_list = set(lst)	# 리스트에서 중복값 제거하는 법
lst = list(set_list)	

lst.sort()
lst.sort(key = len)

for i in lst:           # 한 줄 씩 출력
    print(i)
    

